# 项目安全说明

## 数据边界

本仓库不得包含真实社群消息、聊天导出、SQLite 数据库、用户身份信息、访问令牌、API Key、服务器凭证或生产日志。

DeepSeek 适配器仅用于模拟数据、公开数据或经过公司明确批准的数据。当前不得把企微官方群、民间微信群或微伴助手中的真实消息发送给 DeepSeek API，即使消息已经脱敏。

正式生产分析必须切换到公司批准的私有模型服务。

## 凭证管理

- 开发密钥通过本地环境变量 `DEEPSEEK_API_KEY` 提供。
- 不在代码、文档、测试样本、提交记录或问题讨论中粘贴密钥。
- `.env` 和常见密钥文件已被 `.gitignore` 排除。
- 如果密钥被误提交，应立即在服务商后台撤销并重新生成，不能只删除最新文件。

## 提交前检查

- 确认没有数据库、导出文件、日志或真实消息样本。
- 确认测试使用模拟文本。
- 确认新增外部调用不会绕过数据分类检查。
- 确认所有接口地址和凭证均可通过配置替换。

## 安全问题处理

发现数据泄露、密钥暴露或越权访问时，应停止相关任务，保存必要的非敏感故障信息，并联系项目最终需求负责人和技术运行负责人处理。

