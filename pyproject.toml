[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ninebot-social-listening"
version = "0.1.0"
description = "Ninebot community listening and risk alert system"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

